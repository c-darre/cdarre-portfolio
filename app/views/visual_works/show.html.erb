<% content_for :title, @visual_work.title %>
<% content_for :meta_description,
   truncate(@visual_work.description.to_s.presence || "Création visuelle : #{@visual_work.title}",
            length: 155, separator: " ") %>
<% if @visual_work.images.attached? %>
  <% content_for :og_image, request.base_url + url_for(@visual_work.images.first) %>
<% end %>

<div data-controller="lightbox">
  <section class="screen" aria-label="<%= @visual_work.title %>">
    <span class="corner corner-tl mono">Galerie · Pièce</span>
    <span class="corner corner-tr mono"><%= category_label(@visual_work.category) if @visual_work.category.present? %></span>

    <article class="visual-show">
      <% if @visual_work.category.present? %>
        <p class="eyebrow mono"><%= category_label(@visual_work.category) %></p>
      <% end %>
      <h1><%= @visual_work.title %></h1>
      <% if @visual_work.description.present? %>
        <p class="desc"><%= @visual_work.description %></p>
      <% end %>

      <% @visual_work.images.each do |image| %>
        <figure>
          <button type="button" class="shot"
                  data-action="lightbox#open"
                  data-src="<%= url_for(image) %>"
                  data-alt="<%= attachment_alt(image, fallback: @visual_work.title) %>"
                  aria-label="Agrandir : <%= attachment_alt(image, fallback: @visual_work.title) %>">
            <%= image_tag image, loading: "lazy",
                  alt: attachment_alt(image, fallback: @visual_work.title) %>
          </button>
        </figure>
      <% end %>

      <p><%= link_to "← Retour à la galerie", visual_works_path, class: "mono" %></p>
    </article>
  </section>

  <%= render "shared/lightbox" %>
</div>
