<%# edit.html.erb %>
<% content_for :title, "Éditer la section — Admin" %>
<h1 class="h3 mb-4">Éditer une section — <%= @section.case_study.title %></h1>
<div class="row"><div class="col-lg-9">
  <%= render "form", section: @section, url: admin_section_path(@section) %>
</div></div>
